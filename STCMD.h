#ifndef STCMD_H
#define STCMD_H
#include <time.h>
#include <string>
#include <iostream>
#include <fstream>
#define length(a) ( sizeof ( a ) / sizeof ( *a ) )
using namespace std;

void wait (int seconds);
static int SIZE = 4;

#endif